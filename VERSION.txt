JSON-UI-Maker Patch v30
Build: v30-explorer-panel-lock

EXPLORER MOBILE:
- lista de itens em painel fixo completo no lado direito
- altura total da tela, sem limite antigo de 381px
- scroll vertical e horizontal nativo no Android
- arrastar a lista nao seleciona itens por acidente
- painel pode ser recolhido e reaberto pelo botao ITENS
- alvos touch maiores e contador de itens

PANEL LOCK:
- nova opcao Panel Lock (Smart Align) nas configuracoes
- alinha itens do mesmo painel por bordas e centros
- tambem alinha ao centro e as bordas do painel pai
- raio de encaixe configuravel em Panel Lock Radius
- guias azuis mostram o eixo sincronizado durante o arraste
- funciona junto ou separado do Grid Lock

SHOW GRID:
- restaurado o comportamento original de SebTheSigma/JSON-UI-Maker
- restaurado o grid 2x2 permanente que marca o centro da preview
- restaurada a bolinha azul no centro do item enquanto ele e movido
- restaurada a cor original das grades: rgb(55, 55, 55), linhas de 2px
- painel selecionado mantem a grade visivel quando Show Grid esta ativo
- ao mover um item, a grade do painel pai aparece durante o arraste
- removida a camada magenta persistente que cobria detalhes das imagens
- removida a varredura de todos os elementos a cada selecao/arraste
- preservados os pontos, handles e hit testing originais

LAYER ORDER CORRIGIDA:
- BACKGROUND: z-index 10, EDITAVEL
- CONTENT SLOT: z-index 20
- BORDER: z-index 60, FIXO
- HEADER: z-index 120, EDITAVEL

BACKGROUND:
- nao e mais ragFixedDecorative
- pointer/touch ativo
- resize handle ativo
- pode mover e redimensionar normalmente
- auto-fit acontece apenas UMA vez na criacao
- depois o sistema nao sobrescreve posicao/tamanho do usuario
- botao AJUSTAR AO CONTENT SLOT nas propriedades

EXPLORER:
- elementos especiais ganham nomes reais:
  BACKGROUND
  BORDER
  HEADER
  HEADER TEXTURE
  CONTENT SLOT
- header aparece explicitamente na lista

Mantem V26 assets Nordic built-in, V25 Border System, V24 copy/paste fix,
Text Field/Dropdown/Toggle/Slider, clipping, touch, grid e sidecar nativo.
