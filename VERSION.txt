JSON-UI-Maker Patch v37
Build: v37-role-scoped-nine-slice

9-SLICE SEPARADO POR FUNCAO:
- 9-slice automatico agora existe somente dentro do componente de botao
- background usa o PNG puro, sem herdar 9-slice automatico de botao
- header usa o PNG puro, sem herdar 9-slice automatico de botao
- borda e imagem normal tambem nao recebem o 9-slice automatico
- sidecar JSON selecionado manualmente continua sendo respeitado
- o mesmo PNG pode ser usado como botao e background com render diferente

MIGRACAO DA V36:
- detecta sidecars marcados como __ragAutoGenerated
- remove o sidecar automatico do estado global da textura
- remove o JSON automatico incorreto salvo no localStorage
- conserva os dados apenas para renderizacao de botoes
- nao exige que o usuario limpe manualmente os assets importados

Mantem V35 seletor de funcao da imagem, V34 botoes compostos, V33 fallbacks,
V32 Selection Drag Lock, V31 round-trip e V30 Explorer Mobile + Panel Lock.
